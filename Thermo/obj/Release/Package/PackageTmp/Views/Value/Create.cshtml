@model Thermo.Models.Value

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Value</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.Val)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Val)
            @Html.ValidationMessageFor(model => model.Val)
        </div>

       

        <div class="editor-label">
            @Html.LabelFor(model => model.EquipementID, "Equipements")
        </div>
        <div class="editor-field">
            @Html.DropDownList("EquipementID", String.Empty)
            @Html.ValidationMessageFor(model => model.EquipementID)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
