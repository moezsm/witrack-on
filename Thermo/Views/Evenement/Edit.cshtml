@model Thermo.Models.Evenement

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Evenement</legend>

        @Html.HiddenFor(model => model.EvenementID)

        <div class="editor-label">
            @Html.LabelFor(model => model.EvenementType)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.EvenementType)
            @Html.ValidationMessageFor(model => model.EvenementType)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
