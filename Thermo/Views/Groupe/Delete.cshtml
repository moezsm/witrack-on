@model Thermo.Models.Groupe

@{
    ViewBag.Title = "Delete";
}

<div class="alert alert-dismissable alert-danger">
    <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
    <h3>Delete!</h3>

    <p>Are you sure you want to delete this?</p>
    <br>
    <p><a data-toggle="modal" class="btn btn-danger" href="#myModal">What to do?</a></p>

</div>
<fieldset>
    <legend>Groupe</legend>
        <div class="col-md-9">
        <div class="tab-container tab-primary">
            <ul class="nav nav-tabs">
                <li class="active"><a href="#home2" data-toggle="tab">@Html.DisplayNameFor(model => model.Name)</a></li>
                
            </ul>
            <div class="tab-content">
                <div class="tab-pane active" id="home2">
                    <p>@Html.DisplayFor(model => model.Name)
                    </p>
                </div>
                
            </div>
        </div>
    </div>
    
    
</fieldset>
@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    <p>
        <input type="submit" class="btn btn-danger " value="Delete" /> 
        <a class="btn btn-primary" @Html.ActionLink("Back to List", "Index")
    </p>
}
<!-- Modal -->
<div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                <h4 class="modal-title">Delete Help!</h4>
            </div>
            <div class="modal-body">
                <p>Here what you can do <i>Be carfull!!</i> delete action is irriversible.</p>
                <ol>
                    <li> If you really want to delete this item clik on delete button.</li>
                    <li>Click on back to list to cancel.</li>
                    
                </ol>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>

            </div>
        </div>
        <!-- /.modal-content -->
    </div>
    <!-- /.modal-dialog -->
</div>
<!-- /.modal -->
